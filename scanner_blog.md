# 🚀 나만의 짱 쎴 포트 스캐너 만들기: 코드 분석 & 실전 활용 가이드 🛠️

안녕하세요! 오늘은 **포트 스캐너**라는 IT 도구를 만들고, 그 코드를 자세히 분석해볼 거예요. 이 도구는 네트워크 서버의 열린 포트를 탐지하고, 취약한 서비스를 경고해주는 역할을 합니다. 🚨  
코드는 Python으로 작성되었고, **명령행 인자**, **멀티스레드**, **색상 출력** 등 다양한 기능이 포함되어 있어요.  
이 글에서는 코드의 핵심 원리와 실전 활용법을 **초보자도 이해할 수 있도록** 설명해드릴게요!  

---

## 🧠 1. 코드 개요: 포트 스캐너란 무엇인가요?

포트 스캐너는 **네트워크 서버의 열린 포트**를 탐지하는 도구입니다.  
예를 들어, `127.0.0.1`이라는 IP 주소의 서버가 `80` 포트(웹 서버)를 열고 있다면, 이 도구는 이를 감지하고 **취약점 경고**를 제공합니다.  
이 코드는 **멀티스레드**로 동작해 빠른 스캔이 가능하며, **취약 포트**를 강조하는 색상 출력 기능도 있습니다.  

---

## 🛠️ 2. 코드 분석: 핵심 기능 파헤치기

### 📌 2-1. CLI 설정: 명령행 인자 처리 📱

```python
parser.add_argument('-t', '--target', required=True, help="스캔할 타겟 IP 주소")
parser.add_argument('-p', '--ports', default="21,22,80,135,433,445,3306,8080,11434", help="스캔할 포트 번호들")
```

- **명령행 인자**로 IP 주소와 포트를 입력받습니다.  
  예: `python scanner.py -t 127.0.0.1 -p 21,80,443`  
- **포트 범위**도 지원합니다. 예: `1-1000` → 1~1000 포트 모두 스캔 가능!

### 📌 2-2. 취약 포트 목록: 위험 신호 🚨

```python
VULNERABLE_PORTS = {
    21: "FTP (암호화 안 됨! 비밀번호 도청 위험)",
    23: "Telnet (절대 사용 금지! 해킹 1순위)",
    ...
}
```

- **취약한 포트**를 사전 형식으로 정의해두고, 스캔 시 경고 메시지를 출력합니다.  
- 예: `21` 포트는 FTP로, 암호화가 없어 **비밀번호 도청** 위험이 있습니다.

### 📌 2-3. 스캔 함수: 포트 상태 확인 🔍

```python
def scan_port(ip, port):
    try:
        sock = socket.socket(...)  # 소켓 생성
        sock.settimeout(1)  # 타임아웃 설정
        result = sock.connect_ex(...)  # 연결 시도
        if result == 0:  # 포트가 열려 있으면
            print(f"{Fore.GREEN}[+] Port {port} OPEN{Fore.RESET}")
            if port in VULNERABLE_PORTS:
                print(f" └── {Fore.RED}[🚨 취약점 경고] {VULNERABLE_PORTS[port]}{Fore.RESET}")
    except:
        pass
```

- **소켓(socket)**을 사용해 포트에 연결 시도합니다.  
- `connect_ex`는 연결 성공 시 `0`을 반환하고, 실패 시 `1` 이상을 반환합니다.  
- **색상 출력**을 위해 `colorama`를 사용해 녹색(OPEN), 빨강(취약점)으로 강조합니다.

### 📌 2-4. 멀티스레드: 빠른 스캔 🚀

```python
with concurrent.futures.ThreadPoolExecutor(max_workers=100) as executor:
    for port in target_ports:
        executor.submit(scan_port, target_ip, port)
```

- **100개의 스레드**를 동시에 실행해, **다중 포트를 병렬 처리**합니다.  
- 이는 스캔 속도를 빠르게 만듭니다.  
- **주의**: 너무 많은 스레드는 시스템 부하를 줄 수 있으니 `max_workers` 값을 조절해야 합니다.

---

## 🧪 3. 실전 활용: 어떻게 사용할 수 있을까요?

### ✅ 사용법 예시

```bash
python scanner.py -t 192.168.1.1 -p 21,22,80,443
```

- `192.168.1.1` IP 주소의 서버를 대상으로, `21`, `22`, `80`, `443` 포트를 스캔합니다.  
- 결과는 다음과 같은 형태로 출력됩니다:

```
[+] Port 22 (ssh): OPEN
 └── [🚨 취약점 경고] Telnet (절대 사용 금지! 해킹 1순위)
```

### 🔍 주의 사항

- **네트워크 보안**을 위반할 수 있으니, **자신의 네트워크 내부만** 스캔하세요.  
- **취약 포트**는 보안 취약점을 나타내므로, **즉시 시스템 업데이트**를 권장합니다.

---

## 📚 4. 핵심 원리 정리: 왜 이렇게 작동하는가?

| 기능 | 설명 |
|------|------|
| `socket` | 네트워크 연결을 위한 기본 라이브러리 |
| `argparse` | 사용자 입력을 처리해 CLI를 편리하게 만듭니다 |
| `ThreadPoolExecutor` | 병렬 처리로 스캔 속도를 빠르게 합니다 |
| `colorama` | 결과를 시각적으로 구분해 독자에게 명확하게 전달합니다 |

---

## 🧠 5. 마무리: 포트 스캐너의 중요성

포트 스캐너는 **네트워크 보안 강화**에 필수적인 도구입니다.  
이 코드를 통해 **취약 포트를 탐지**하고, **즉각적인 대응**을 할 수 있어요.  
하지만 **법적 허가 없이 타인의 네트워크를 스캔**하는 것은 **해킹**으로 간주될 수 있으니 주의하세요!  

---

## 📌 참고 자료

- [Python socket 모듈 공식 문서](https://docs.python.org/3/library/socket.html)
- [argparse 모듈 사용법](https://docs.python.org/3/library/argparse.html)
- [Colorama GitHub 저장소](https://github.com/tmbo/colorama)

---

📌 **요약**:  
이 포트 스캐너는 **명령행 인자**, **멀티스레드**, **취약 포트 경고** 기능을 갖춘 강력한 도구입니다.  
코드를 이해하고 활용하면, **네트워크 보안 강화**에 큰 도움이 될 거예요!  

**아직도 궁금한 점이 있거나, 코드를 수정하고 싶다면 언제든지 물어보세요!** 💬  
#IT #网络安全 #Python #PortScan #SecurityTools